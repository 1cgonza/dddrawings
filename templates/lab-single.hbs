{{> head }}
<main class="{{slug title}}">
  <section id="ddd-container">
    <span id="ddd-loading" class="loading">...</span>
  </section>

  <section id="content">
    {{{ contents }}}
  </section>
</main>

{{#if libraries}}
  {{setLibraries libraries}}
{{/if}}

{{#if scripts}}
  {{setScripts scripts}}
{{/if}}
{{> footer }}